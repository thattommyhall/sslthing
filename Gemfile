# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem "powerdns_pipe", "~> 1.1"

group :development, :test do
  gem 'rubocop', require: false
  gem "minitest"
  gem 'guard'
  gem 'guard-minitest'
end